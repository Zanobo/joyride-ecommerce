<% cache [I18n.locale, current_pricing_options, @product] do %>

    <div class="modal-body modal-dialog" style="background:white;width:75%;margin:auto;">
      <div id="wrapper" class="row" data-hook style="width:70%;margin:auto;">
      <div data-hook="product_show" itemscope itemtype="http://schema.org/Product" >
        <% @body_id = 'product-details' %>
        <div class="columns six alpha" data-hook="product_left_part" style="maright-right:30px;">
          <div class="row" data-hook="product_left_part_wrap">

            <div id="product-images" data-hook="product_images">
              <div id="main-image" data-hook>
                <%= render :partial => 'image' %>
              </div>
              <div id="thumbnails" data-hook>
                <%= render :partial => 'thumbnails' %>
              </div>
            </div>

            <div data-hook="product_properties">
              <%= render :partial => 'properties' %>
            </div>

            <div data-hook="promotions">
              <%= render :partial => 'promotions' %>
            </div>

          </div>
        </div>

        <div class="columns six omega" data-hook="product_right_part" style="margin-left:30px;">
          <div class="row" data-hook="product_right_part_wrap">
            <h2 class="product-title" itemprop="name" style="margin-top:0;"><strong><%= @product.name %></strong></h2>
            <div id="product-description" data-hook="product_description">

              <div style="margin-bottom:10px;">
                By <%= @product.taxons.first.name %>
              </div>

              <div itemprop="description" data-hook="description">
                <%= product_description(@product) rescue Spree.t(:product_has_no_description) %>
              </div>

              <div id="cart-form" data-hook="cart_form">
                <%= render :partial => 'cart_form' %>
              </div>

            </div>

            <%= render :partial => 'taxons' %>

          </div>
        </div>
      </div>
        </div>
    </div>

<% end %>